{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd tia-frontend && npm install && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.py"
    }
  ]
}
